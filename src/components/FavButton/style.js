import styled from "styled-components";

export const Button = styled.button`
	padding: 1rem 0;
	cursor: pointer;
	display: flex;
	align-items: center;
	& svg {
		margin-right: 4px;
	}
	& span {
		font-size: 18px;
	}
`;
